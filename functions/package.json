{
  "name": "neuralgraph-site-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.3.0",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
